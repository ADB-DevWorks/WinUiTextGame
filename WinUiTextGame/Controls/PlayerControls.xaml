<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUiTextGame.Controls.PlayerControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiTextGame.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Blue" Height="100" ColumnSpacing="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="25*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Grid.Row="0" 
                Background="Black"
                Height="20" Width="50"
                Content="Go to Town" Foreground="White"></Button>
        <Button Grid.Column="2" Grid.Row="0" Background="Black"></Button>
        <TextBox AutomationProperties.Name="multi-line TextBox"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 IsSpellCheckEnabled="True" MinWidth="400"
                 Grid.Row="1" Background="White"
                 Grid.Column="1" Grid.ColumnSpan="2" />
    </Grid>
</UserControl>
